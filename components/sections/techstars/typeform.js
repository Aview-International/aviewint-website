import { Widget } from '@typeform/embed-react';

const Typeform = () => {
  return (
    <section
      className="section m-horizontal mt-s3 pt-s4 text-white md:mt-s6 md:pt-s8"
      id="generate-aview"
      data-aos="zoom-in"
    >
      <div className="h-[38rem] w-full">
        <Widget id="ppaUHuXP" style={{ width: '100%', height: '100%' }} />
      </div>
    </section>
  );
};

export default Typeform;
